name = "racun-dapur-ibu-bot"
main = "src/index.ts"
compatibility_date = "2024-07-25"
compatibility_flags = ["nodejs_compat"]

[vars]
ENVIRONMENT = "production"

# Penjadual Otomatik Cron Trigger (Contoh: Setiap 2 jam)
[triggers]
crons = ["0 */2 * * *"]